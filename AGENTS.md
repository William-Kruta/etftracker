This project uses 'uv' package manager. Activate the venv by using this command "source .venv/bin/activate"

Install package with this command: "uv pip install package-name"

Run python scripts like this: "python script.py" (assuming the venv is activate)
If you need to install packages, make sure to update the requirements.txt accordingly.
